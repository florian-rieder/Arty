#:kivy 1.0
#:import kivy kivy

<MetadataItem>:
    Label:
        id: label
        text_size: self.size
        halign: "left"
        valign: "middle"
        padding_x: 40
        size_hint_x: 0.4
    TabTextInput:
        id: text_input
        cursor_color: (0, 0, 0, 1)