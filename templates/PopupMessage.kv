#:kivy 1.0
#:import kivy kivy

<PopupMessage>:

    Label:
        text: root.message
        size_hint: 0.6, 0.2
        pos_hint: {"x":0.2, "top":1}

    Button:
        id: popup_btn
        text: "ok"
        size_hint: 1, 0.4
        pos_hint: {"x":0, "y":0.1}