#:kivy 1.0
#:import kivy kivy

<CollectionToolbar>:

    BoxLayout:
        orientation: 'horizontal'
        size_hint: 1,1

        Button:
            id: home_btn
            text: 'Home'
            on_release: root.to_home_screen()

        Button:
            id: save_btn
            text: 'Save'
            on_release: root.save_coll()

        Spinner:
            id: select_sort
            text: 'Sort'
            values: root.sorting_attributes
            on_text: root.sort_by(select_sort.text)
        
        Button:
            id: filter_select
            text: 'Filter'
            on_release: root.open_filter()

        Button:
            id: select_all
            text: 'Select all'
            on_release: root.select_all()

        Button:
            id: compare_btn
            text: 'Compare'
            on_release: root.compare()
        Button:
            id: export_btn
            text: 'Export PPTX'
            on_release: root.export()
