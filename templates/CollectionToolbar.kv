#:kivy 1.0
#:import kivy kivy

<CollectionToolbar>:

    BoxLayout:
        orientation: 'horizontal'
        size_hint: 1,1

        Button:
            id: compare_btn
            text: 'Compare Selection'
            on_press: root.compare()
        Button:
            id: export_btn
            text: 'Export Selection'
            on_press: root.export()

        Spinner:
            id: select_sort
            text: 'sort by:'
            values: ['title', 'artist', 'datation', 'technique']

            on_text: root.sorting_by(select_sort.text)
