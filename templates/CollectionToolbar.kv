#:kivy 1.0
#:import kivy kivy

<CollectionToolbar>:

    # MDBoxLayout:
    #     adaptive_height: True
    #     # md_bg_color: app.theme_cls.primary_color

    #     MDRectangleFlatIconButton:
    #         id: home_btn
    #         icon: 'home'
    #         text: 'Home'
    #         on_release: root.to_home_screen()

    #     MDRectangleFlatIconButton:
    #         id: save_btn
    #         icon: 'content-save'
    #         text: 'Save'
    #         on_release: root.save_coll()

    #     # Spinner:
    #     #     id: select_sort
    #     #     text: 'Sort'
    #     #     values: root.sorting_attributes
    #     #     on_text: root.sort_by(select_sort.text)
        
    #     MDRectangleFlatIconButton:
    #         id: select_sort
    #         icon: 'sort-ascending'
    #         text: 'Sort'
    #         on_release: root.open_filter()

    #     MDRectangleFlatIconButton:
    #         id: filter_select
    #         icon: 'filter-variant'
    #         text: 'Filter'
    #         on_release: root.open_filter()

    #     MDRectangleFlatIconButton:
    #         id: select_all
    #         icon: 'check-all'
    #         text: 'Select all'
    #         on_release: root.select_all()

    #     MDRectangleFlatIconButton:
    #         id: compare_btn
    #         icon: 'compare'
    #         text: 'Compare'
    #         on_release: root.compare()
        
    #     MDRectangleFlatIconButton:
    #         id: export_btn
    #         icon: 'microsoft-powerpoint'
    #         text: 'Export PPTX'
    #         on_release: root.export()

    MDToolbar:
        # id: toolbar
        title: 'Arty'
        md_bg_color: app.theme_cls.primary_color
        elevation: 15
        left_action_items: [["home", lambda home: root.to_home_screen(), 'Back to home page']]
        right_action_items: [["sort-ascending", lambda x: root.sort_drop(x), 'Sort the collection'],['filter-variant', lambda x: root.open_filter(), 'Filter the images'],['check-all', lambda x: root.select_all(), 'Select all the images, Ctrl+A'],["content-save", lambda x: root.save_coll(), 'Save the collection, Ctrl+S'],['compare', lambda x: root.compare(), 'Compare the selected images'],["microsoft-powerpoint", lambda x: root.export(), 'Create a .pptx with selected images, Ctrl+E']]
