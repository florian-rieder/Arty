#:kivy 1.0
#:import kivy kivy

<CollectionPanel>:
    orientation: 'vertical'

    BoxLayout:
        ImagePreview:
            id: preview
            allow_stretch: True

    BoxLayout:
        id: metadata_container
        orientation: 'vertical'