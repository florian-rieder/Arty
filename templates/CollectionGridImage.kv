#:kivy 1.0
#:import kivy kivy

<CollectionGridImage>:

    # canvas:
    #     Rectangle:
    #         source: root.source
    #         pos: self.pos
    #         size: self.size

    # Image:
    #     id: coll_image
    #     source: root.source
    #     size_hint: 1, None
    #     #height: dp(40)
    size_hint: 1,1
    AnchorLayout:
        anchor_x: 'left'
        anchor_y: 'top'

        CheckBox:
            id: select_image
            color: (0,0,0,0)
            size_hint: (0.1,0.1)
            pos_hint: {'x': 1, 'y': 0.8}
            # background_checkbox_normal: 'resources/blank-check-box.png'#'resources/empty.png'
            background_checkbox_down: 'resources/check-box.png'
            on_active: root.checkbox_click(self, self.active) 